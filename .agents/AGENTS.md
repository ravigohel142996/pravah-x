# Project Rules

## Razorpay Reference Clean-up Constraint
- **Constraint**: After September 3rd or 4th, 2026, ensure the name "razorpay" (in any case, e.g., "Razorpay", "razorpay") is NOT used in new code, comments, or documentation.
- **Goal**: When working on the codebase around or after this date, proactively remind the user to refactor and rename existing "razorpay" occurrences to a generic term like "payment_gateway" or "secure_pay" (since the repository is public), and assist them in performing this refactoring.
